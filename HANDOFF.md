# HANDOFF — Калькулятор стоимости крашеных фасадов (МДФ / шпон)

Документ передачи по проекту `proschet_Fasad`.
Ветка разработки: `claude/mdf-facade-cost-calculator-v7qmfw`.

---

## 1. Что это

Калькулятор для просчёта стоимости крашеных фасадов из МДФ и шпона в
стилистике сайта **«Среда»** (mebelart.net).

- Один самодостаточный файл **`index.html`** — работает офлайн, без сервера,
  установки и интернета. Открывается двойным кликом в браузере
  (Chrome / Edge / Safari).
- Вся логика (расчёт, хранение, печать, генерация договора) — на чистом
  JS/CSS внутри файла, без внешних библиотек и CDN.

Файлы в репозитории:
- `index.html` — приложение.
- `README.md` — инструкция для пользователя.
- `HANDOFF.md` — этот документ.

---

## 2. Что было сделано (по итерациям запросов)

### Итерация 1 — базовый калькулятор
- Шапка со стилем бренда «Среда» (минимализм, тёплые нейтральные тона,
  чистая типографика).
- Поле **«Наименование / заказчик»** — при вводе документ автосохраняется.
- Поле **«Наименование фасада»**.
- Таблица позиций с **автонумерацией**: наименование, высота, ширина,
  толщина (мм), количество (шт), площадь, цена, сумма.
- **Навигация стрелками** ↑ ↓ ← → и Enter, как в Excel; авто-добавление
  строки при заполнении последней.
- Кнопка **«Печать / PDF»**, подвал с данными заказчика, строкой
  «С размерами согласен» и местом для подписи.
- Редактируемый прайс в интерфейсе (сохраняется в браузере).

### Итерация 2 — интеграция прайса из Excel
- Разобран присланный Excel. Ключевой вывод: **цена ₽/м² зависит и от типа
  отделки, и от толщины** (16/19/22 мм). Прайс встроен по столбцам
  «наименование / цена м²».

### Итерация 3 — расширение модели
- **Несколько видов фасадов** в одном документе (кнопка «＋ Добавить вид
  фасада» — отдельная табличка со своим названием/отделкой/списком).
- **Автоподстановка наименования** детали = «наименование фасада · отделка»
  (с ручным переопределением).
- Раздел прайса **«Покраска материала заказчика»** (фикс. ₽/м², без толщины):
  фреза 6580, мыло 4500, глянец 7280, шпон файнлайн 8600, шпон натуральный
  11600, шпон рамка 13400.
- **Галочка «обратка»** у каждой детали: строка становится **жирной** +
  добавляется покраска обратной стороны (краска 4500 ₽/м², шпон 5500 ₽/м²).
- **Минимальная площадь 0,7 м²** по всему списку деталей каждого вида фасада.

### Итерация 4 — вёрстка таблицы
- Фиксированные ширины колонок (`table-layout: fixed` + `colgroup`):
  размеры под ~6 знаков, количество ~5, толщина ~4, «Наименование» — шире.

### Итерация 5 — плотность
- Шрифт уменьшен на 1px, вертикальные отступы строк сокращены вдвое.

### Итерация 6 — хранение и сохранение
- В памяти браузера хранятся **10 последних расчётов** (индекс по свежести,
  авто-удаление самого старого).
- Кнопка **«Сохранить файл»** — выгрузка расчёта отдельным самодостаточным
  `.html`-документом (открывается уже заполненным, с встроенным прайсом);
  где поддерживается — системный диалог «Сохранить как…».

### Итерация 7 — телефон
- Поле **«Телефон»** рядом с именем заказчика, предзаполнено `+7`; выводится
  в подвале и сохраняется с документом.

### Итерация 8 — договор
- Кнопка печати формирует **два документа**: сначала **бланк заказа**, затем
  **договор** (шаблон ИП Малюга, присланный RTF) с подставленными данными:
  ФИО заказчика, телефон, № и дата, сумма по смете. Договор печатается через
  скрытый iframe → отдельный PDF.

### Итерация 9 — печать
- Компактная печатная версия бланка под A4: мельче шрифт и поля, узкие
  числовые колонки, широкое «Наименование» (перестало обрезаться). Ширины
  колонок вынесены в CSS-классы, чтобы печать могла их переопределять.

---

## 3. Формулы расчёта

```
Сумма детали   = площадь (В×Ш, м²) × (цена ₽/м² + обратка, если отмечена) × кол-во
Сумма по виду  = Σ по деталям; если суммарная площадь < мин. площади,
                 добор (мин.площадь − площадь) × цена отделки
Итого          = Σ по всем видам фасадов
```

- «Изготовление» — цена ₽/м² по толщине детали (16/19/22, берётся ближайшая).
- «Покраска заказчика» — фиксированная ₽/м².
- Обратка: для крашеных отделок — по цене краски, для шпона — по цене шпона.
- Наценка (%) и все цены редактируются в блоке «Прайс-лист».

---

## 4. Прайс (значения по умолчанию)

**Изготовление (МДФ + покраска), ₽/м² по толщине** — из Excel:

| Отделка | 16 мм | 19 мм | 22 мм |
|---|---:|---:|---:|
| Мыло (мат) | 6 445 | 6 825 | 7 254 |
| Фреза (мат) | 9 295 | 9 675 | 10 001 |
| Глянец | 8 853 | 9 233 | 9 663 |
| Фрезерованный глянец | 11 703 | 12 083 | 12 513 |

**Покраска материала заказчика, ₽/м² (фикс.):**
фреза 6 580 · мыло 4 500 · глянец 7 280 · шпон файнлайн 8 600 ·
шпон натуральный 11 600 · шпон рамка 13 400.

**Параметры:** обратка краска 4 500 ₽/м², обратка шпон 5 500 ₽/м²,
мин. площадь 0,7 м², наценка 0 %.

---

## 5. Проблемы, с которыми столкнулись, и как решены

- **Прайс не был единой ценой за м².** По первому впечатлению — просто цена
  ₽/м². При разборе Excel выяснилось, что цена зависит от **отделки + толщины**
  (мыло/фреза/глянец/ф-глянец × 16/19/22). Модель прайса переделана под это,
  колонка «толщина» стала реально влиять на цену.
  → Урок: не фиксировать модель цены до разбора исходного прайса.

- **Правка файла падала с «String to replace not found».** Из-за
  кириллицы/невидимых различий точечная замена не находила строку. Решение —
  переписать файл целиком (`Write`), а не бороться с точечной заменой.

- **Не было библиотек для чтения исходников.** `openpyxl` (Excel) и `striprtf`
  (RTF договора) отсутствовали — доустановлены через `pip` для разбора.

- **Шпон отсутствовал в первом Excel.** Изначально прайс был только по МДФ.
  Позиции по шпону добавлены во второй итерации из отдельного списка цен
  заказчика.

- **Печать не совпадала с экраном** (крупный шрифт, большие поля,
  обрезалось «Наименование»). Причина: A4 уже экрана, а ширины колонок были
  заданы инлайн и не переопределялись для печати. Решение — вынести ширины в
  CSS-классы и добавить компактные правила `@media print`.

- **Ограничения браузера по «сохранению файлов».** Браузер не может молча
  писать файлы на диск. «Автосохранение» реализовано в `localStorage`
  (10 последних), а явное сохранение — через кнопку (диалог «Сохранить как…»
  или загрузка). Автоскачивание на каждое нажатие клавиши сознательно не
  делалось.

- **Два PDF за одно нажатие.** Без сторонних PDF-библиотек и при строгом
  офлайне два отдельных PDF получаются только через два диалога печати подряд
  (бланк — текущая страница, договор — скрытый iframe). Это осознанный
  компромисс.

---

## 6. Результат

- Рабочий калькулятор в одном файле `index.html`, полностью офлайн.
- Реализованы все запрошенные функции: брендовый стиль, автосохранение,
  автонумерация, ввод размеров/толщины/количества, авто-цена из прайса
  (отделка × толщина), навигация стрелками, несколько видов фасадов,
  автоимена деталей, галочка «обратка» (жирная строка + доплата), мин.
  площадь 0,7 м², телефон `+7`, хранение 10 последних расчётов, сохранение
  отдельным документом, печать бланка + договора (два PDF), компактная печать.
- Логика проверена изолированными тестами: расчёт цены с обраткой/мин.
  площадью, лимит «10 последних», подстановка полей в договор — корректны.
- Всё закоммичено и запушено в ветку
  `claude/mdf-facade-cost-calculator-v7qmfw`.

## 7. Возможные доработки (не реализованы, по запросу)

- Маска ввода телефона `+7 (___) ___-__-__`.
- Поля «Адрес» и «E-mail» заказчика в шапку — с подстановкой в договор.
- Мин. площадь на весь документ, а не на каждый вид фасада.
- Логотип-картинка и реквизиты в печатной шапке бланка.
- Импорт прайса из Excel прямо в интерфейсе.
